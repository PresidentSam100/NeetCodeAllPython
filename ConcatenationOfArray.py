class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        concat = []
        for num in nums:
            concat.append(num)
        for num in nums:
            concat.append(num)
        return concat
